function PageHeader({ subtitle, title }) {
    return (
        <section id="pageHeader">
            <h3>{subtitle}</h3>
            <h1>{title}</h1>
        </section>
    )
}

export default PageHeader
